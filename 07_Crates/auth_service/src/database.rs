pub enum Status{
    Connected,
    Interrupted,
}

pub fn connect_to_database() -> Status {
    // logic of connection
    Status::Connected
}

pub fn get_user() {
    // fetch the user from the database and return
}
